steps:
# Configure the Hello World cloud function
- name: 'gcr.io/cloud-builders/gcloud'
  args:
    - functions
    - deploy
    - --runtime=go113
    - --trigger-http
    - --source=./functions/genetic # Function source files
    - --entry-point=Generate # Function entry point
    - --region=europe-west1
    - --allow-unauthenticated # This function is publicly available 
    - genetic # Function name
  waitFor: ['-']
  id: 'genetic'